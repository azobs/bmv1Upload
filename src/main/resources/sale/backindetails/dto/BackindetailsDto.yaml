type: object
description: 'A Backin in the system which represent articles return back by a client after delivery'
properties:
  id:
    type: integer
    format: int64
    readOnly: true # Returned by GET, not used in POST/PUT/PATCH
  bidQuantity:
    type: number
    format: double
    example: 0
  bidComment:
    type: string
  bidArticle:
    type: object
    $ref: '../../../product/article/dto/ArticleDto.yaml'
  bidBackin:
    type: object
    $ref: '../../backin/dto/BackinDto.yaml'
  
